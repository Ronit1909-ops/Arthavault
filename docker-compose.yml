version: "3.9"
services:
  redis:
    image: redis:7-alpine
    container_name: upi_tracker_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 20 1 --loglevel warning

volumes:
  redis_data:
