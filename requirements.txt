# FastAPI core
fastapi==0.109.2
uvicorn[standard]==0.27.1

# MongoDB async driver
motor==3.3.2
pymongo==4.6.2

# Redis async client
redis==5.0.1          # aioredis 2.x merged into redis-py >=4.2 with asyncio support

# OAuth + HTTP
authlib==1.3.0
httpx==0.26.0          # required by Authlib async transport

# JWT
PyJWT==2.8.0

# Pydantic settings (env management)
pydantic==2.6.1
pydantic-settings==2.2.1
email-validator==2.1.0   # required by Pydantic's EmailStr

# Password / crypto helpers (used internally)
passlib[bcrypt]==1.7.4
python-multipart==0.0.9  # for form data

# Date utilities
python-dateutil==2.9.0

# Env file loading (dev convenience)
python-dotenv==1.0.1

# ── PDF Parsing ───────────────────────────────
pdfplumber==0.10.4        # PDF table extraction (wraps pdfminer.six)
python-magic==0.4.27       # MIME type validation (Linux/macOS); use python-magic-bin on Windows
pikepdf                   # Decrypt password-protected PDFs

# ── Testing ───────────────────────────────────
pytest==8.1.0
pytest-asyncio==0.23.5
